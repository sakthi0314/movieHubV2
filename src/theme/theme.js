import { createMuiTheme } from "@material-ui/core";

// Custom theme
const theme = createMuiTheme({
  palette: {
    type: "dark",
    primary: {
      main: "rgb(245, 197, 24)",
    },
  },
});

export default theme;
